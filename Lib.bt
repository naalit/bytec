extern {
    package bytecoder;

    import java.util.Random;
}

enum Bool {
    True,
    False,
}

extern class Random {
    constructor();
    fn next_int(max: i32): i32 = "nextInt";
}

fn getArr(): [i32] = arr;

let arr: [i32] = [];
fn add_arr(i: i32) {
    arr.push(i);
}
